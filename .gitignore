# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.python-version

# Build / dist
build/
dist/
*.egg-info/
*.egg

# Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# Env / secrets
.env
.env.local

# Run artifacts (Phase 2+)
runs/
!runs/.gitkeep

# Node (Phase 6+)
node_modules/
dist/
.vite/
.superpowers/

# uv.lock IS committed (this is an application, not a library — lock the
# resolved deps for CI reproducibility and so anyone cloning gets the
# exact graph that was tested).

# Internal AI working docs — specs / plans driven by claude-code,
# not for public consumption. Files stay locally; just untracked.
docs/superpowers/

# Playwright / e2e
test-results/
playwright-report/
/tmp/web-dogfood/
